# Database Configuration File
# This file contains the database connection details used by the tic-tac-toe app

version: "3.8"

services:
  app:
    build: ./tic-tac-toe/tempdir
    container_name: ttt_app
    ports:
      - "3308:3306"
    environment:
      # External MySQL Database Configuration
      DB_HOST: yttlzg.h.filess.io
      DB_PORT: 3307
      DB_NAME: ttt_app_listweight
      DB_USER: ttt_app_listweight
      DB_PASSWORD: 15c5b65ce12d927dd44652183d83a44196a54474
    restart: unless-stopped
